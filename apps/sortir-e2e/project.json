{
  "root": "apps/sortir-e2e",
  "sourceRoot": "apps/sortir-e2e/src",
  "projectType": "application",
  "targets": {
    "e2e": {
      "executor": "@nrwl/cypress:cypress",
      "options": {
        "cypressConfig": "apps/sortir-e2e/cypress.json",
        "devServerTarget": "sortir:serve:development"
      },
      "configurations": {
        "production": {
          "devServerTarget": "sortir:serve:production"
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/sortir-e2e/**/*.{js,ts}"]
      }
    },
    "version": {
      "executor": "@jscutlery/semver:version",
      "options": {
        "preset": "angular",
        "commitMessageFormat": "chore(${projectName}): release version ${version}"
      }
    }
  },
  "tags": [],
  "implicitDependencies": ["sortir"]
}
